# Raw source material — heavy, local-only. Pipeline reads from here, output is committed.
RESSOURCES/

# Legacy v1 stub
castle-clasher-v1/

# v2 built artifacts (keep sources, ignore output)
castle-clasher-v2/playable-bundle.html
castle-clasher-v2/node_modules/

# Editor / OS
.DS_Store
*.swp
node_modules/
dist/playable.html

# Secrets
.env
.env.local
.env.*.local

# Pipeline runs — vidéos source + sandbox de l'agent restent locales
input/*/input/*.mp4
input/*/input/*.mov
input/*/SANDBOX/
# Note : input/<jeu>/shots/ reste committée pour que l'humain dev review l'avancement
