# Assets brief — toutes variations

| V | Slug | Nouveaux assets | Origine | Notes |
|---|---|---|---|---|
| V1 | cold-open | aucun | existing | réutilise BLUE_CASTLE / BOMB / explosion particles existants ; juste retiming du script + flash overlay procédural |
| V2 | aim-preview | aucun | existing | pure modif de `scene_interior/aim.js` — trajectoire dotted déjà disponible, on la rend persistante hors-drag |
| V3 | night-storm | aucun | existing | overrides procéduraux dans `scene_exterior/index.js` (sky gradient, lightning flash) — pas de PNG |
| V4 | fail-tease | aucun | existing | endcard background = ENDCARD_BG existant ; juste copy + couleurs dans `playable/endcard.js` |

→ Aucune génération Scenario MCP nécessaire. Aucune extraction frame. Bundle inchangé.
