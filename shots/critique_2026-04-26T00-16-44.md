<!-- critique.mjs — model: google/gemini-3.1-pro-preview — 2026-04-26T00:16:44.528Z -->
<!-- webm: /home/samiennedoui/hackathon-voodoo-2026/shots/gameplay_2026-04-26T00-15-41.webm -->

## ✅ Matches well
* **Character Roster:** The character designs for the red imp, green goblin, and skeleton are visually consistent between the playable and the source frames.
* **Internal Tower Concept:** The visual representation of the tower's interior—black brick background, wooden platforms, and external silhouettes—is relatively well replicated in the playable ad.
* **Health System Premise:** The basic concept of Blue vs. Red structural health bars dropping upon taking damage is present in both.

## ❌ Discrepancies
* **[P0] Missing core aiming mechanic** — At `00:05` in the playable recording, the hand tutorial only prompts the user to *click/place* units. In the source (`clip2_0023`, `clip2_0068`), the core interaction requires dragging from the unit to aim a shot, indicated by a dotted trajectory line.
* **[P0] Missing opponent / False context** — At `00:00` in the playable, the user's castle is alone on screen getting bombarded from off-screen, followed by a jarring cut to the interior. The source (`clip2_0001`, `clip2_0046`) clearly establishes a 1v1 battle scene with two castles facing each other.
* **[P1] Completely different environment art** — The playable (`00:00`) uses a flat snowy/rainy mountain background. The source (`clip2_0001`) features a lush green forest/jungle landscape.
* **[P1] Incorrect castle base (locomotion)** — At `00:00` in the playable, the castle sits on four round wheels. In the source (`clip2_0001`), the castle moves on heavy tank-style continuous tracks.
* **[P1] Incorrect castle roof colors** — At `00:00` the playable has blue conical roofs. The source game (`clip2_0001`) relies on red domed roofs for the left-side castle.
* **[P1] Top HUD UI mismatch** — At `00:00` in the playable, the health bars are thin, basic, and contain plain text inside the bars. The source HUD (`clip2_0001`) uses thick block colors (Blue/Black/Red), features a massive central "VS" graphic, and places the percentage text loudly *beneath* the castle icons.
* **[P2] Missing combat VFX** — The source game features dynamic damage text (e.g., "-140" in `clip2_0158`), varied projectiles (crows dropping bombs, rockets), and elaborate explosion particles. The playable relies on a generic puff of smoke.

## 🔧 Fix list (ordered by impact)
1. **Implement Drag-to-Aim Logic:** In your JS event listener, change the unit interaction from a simple `onClick`. On `pointerdown` over a placed unit, instantiate a trajectory rendering function. On `pointermove`, calculate the arc and draw a dashed line using Canvas2D `arc()` (fill: `#FFFFFF`, diameter: ~10px, spacing: ~20px). Fire projectile on `pointerup`. 
2. **Rebuild the Scene Composition:** Zoom the camera out significantly. Add the enemy castle placeholder on the right side of the canvas to establish the 1v1 context before transitioning to the internal view.
3. **Swap Castle Sprites:** 
    * Replace the 4 circular wheel sprites with the 2 tank tread sprites.
    * Replace the blue roof sprites with the red variant.
4. **Update the Top UI Container:**
    * Change the Left Health Bar (`#008CFF`) and Right Health Bar (`#E30000`) to be thicker (~40px height).
    * Add a 100px wide black (`#000000`) spacer in between the bars.
    * Render a "VS" image asset absolutely centered over that black spacer.
    * Move the `100%` text elements below the health bars, using a bold web font with a thick black WebKit text stroke (e.g., `-webkit-text-stroke: 3px black; color: white;`).
5. **Update Background Assets:** Replace the current snowy parallax background layers with the green forest variants to match the actual game's palette.

## 🚫 Cannot assess
* **Audio:** Neither the provided video nor screenshots contain audio data to evaluate game UI sounds, SFX, or BGM.
* **Unit Selection UI Logic:** The bottom 3-character selection dock shown in the playable (`00:04`) does not appear in the provided source frames, so I cannot tell if it accurately reflects the game's actual deployment system. 
* **End Card Flow:** Cannot assess store routing logic or if the end card accurately represents actual game-over states.