<!-- critique.mjs — model: google/gemini-3.1-pro-preview — 2026-04-26T01:09:53.089Z -->
<!-- webm: /home/samiennedoui/hackathon-voodoo-2026/shots/gameplay_2026-04-26T01-08-24.webm -->

## ✅ Matches well
* Core concept of two moving castles on tank treads attacking each other.
* Character roster is accurate: a red cyclops, a grey skeleton, and a green orc/goblin.
* Castles physically tilt backwards at an angle when taking heavy damage.
* The structure of the top UI element holds the correct elements (Blue/Red health bars, castle icons, "Vs", and percentages).

## ❌ Discrepancies
* **[P0] Wrong mechanic/missing feature** — Playable (00:04 - 00:08) implements a drag-and-drop character placement mechanic with a bottom unit selection UI. Source (`clip2_0023.png`) shows a completely different "slingshot" drag-to-aim trajectory mechanic used directly on the characters, and there is no bottom UI panel.
* **[P0] Wrong mechanic/missing feature** — Playable (00:04) shows the characters empty-handed, performing generic fist-pumping attack animations. Source (`clip2_0023.png`) clearly shows the characters wielding distinct heavy weapons (e.g., skeleton has a blue minigun, orc has a green cannon). 
* **[P1] Visible visual mismatch** — Castle interior textures are completely wrong. Playable (00:04) uses brownish-yellow bricks. Source (`clip2_0023.png`) uses dark grey/almost black brick tiles for the interior.
* **[P1] Visible visual mismatch** — Environment artwork is poorly replicated. Playable (00:00) uses flat, monotonous green shapes for trees/mountains and a solid brown ground. Source (`clip2_0001.png`) features rich parallax backgrounds, rain, clouds, detailed trees, and a cross-section of deep red dirt beneath the surface grass.
* **[P1] Visible visual mismatch** — Top UI styling is flattened. Playable (00:00) uses thin, basic progress bars, a generic font, and places text awkwardly inside icons. Source (`clip2_0001.png`) heavily stylizes the header: thick segmented bars, giant wobbly "Vs", and percentage text placed *underneath* large castle icons.
* **[P2] Minor cosmetic diff** — Simplified castle exterior. Playable (00:00) has empty dark tunnel arches at the base and featureless dark tank treads. Source (`clip2_0001.png`) features detailed wooden double doors and metallic tread gears with spikes.
* **[P2] Minor cosmetic diff** — Inbound projectiles. Playable (00:01) uses generic grey rocks thrown horizontally. Source (`clip2_0001.png`, `clip2_0181.png`) features bombs dropped by black birds or rockets raining down from above.

## 🔧 Fix list (ordered by impact)
1. **Scrap Bottom UI & Implement Drag-to-Aim**: Remove the unit spawning dock at the bottom of the canvas. Attach `pointerdown` and `pointermove` listeners to the characters. Dragging backward should calculate and render a visual targeting trajectory made of ~7 white dots (decreasing from 8px to 4px radius) + a final target indicator (Source: `clip2_0023.png`). Trajectory rendering should be instantaneous (0ms delay).
2. **Equip Character Weapons**: Update the character sprite sheets to include their signature weapons as seen in the source. Attach a red cannon to the cyclops, a blue multi-barrel gun to the skeleton, and a green launcher to the orc. Bind the weapon rotation to follow the aim angle from the aforementioned trajectory mechanic.
3. **Change Interior Brick Tiles**: Swap the interior wallpaper background from the current brown color (approx `#5a4b32`) to the dark grey/black brick asset (base color `#1a1a1a` with `#111111` mortar lines) to match `clip2_0023.png`.
4. **Upgrade Environment Canvas Layers**: Replace the flat background elements. Layer multi-toned green hills (`#9ab78c`), adding slow-moving cloud sprites (translating X at ~10-15px/second). Change the solid ground fill layer to a deep subterranean red (`#5c1a17`) topped with a wavy dirt crust.
5. **Restructure Top UI Header**: Increase the CSS/Canvas height of the top blue/red health bars to ~40px. Swap the generic "VS" and numbers to a thick, cartoony white font with a heavy black outline (`lineWidth: 3px`). Move the `100%` texts to sit *below* the castle icons rather than overlapping them.
6. **Detailed Castle Assets**: Redraw the base of the castle exterior sprite. Add two detailed circular wooden doors to the bottom arches. Insert large, inner rotating silver gear sprites (`> 40px` diameter) inside the tread loops.

## 🚫 Cannot assess
* Cannot evaluate the game/level end-state UI or fail/win card sequences since the source footage cuts off during core combat.
* Cannot compare audio clips, background music, or SFX as neither file provided audio layers.
* Cannot evaluate overall game performance/frame drops as the reference is a 4fps image sequence.