<!-- critique.mjs — model: google/gemini-3.1-pro-preview — 2026-04-26T01:50:48.739Z -->
<!-- webm: /home/samiennedoui/hackathon-voodoo-2026/shots/interactive_2026-04-26T01-49-57.webm -->

## ✅ Matches well
*   **Core gameplay loop:** The overarching concept of a tank-tracked castle moving, tilting into a crater, and engaging in combat is preserved.
*   **Camera transitions:** The timing and logic of transitioning from the macro exterior view to the micro interior tower view upon taking threshold damage is accurate.
*   **Aiming mechanic:** The core interactive functionality of dragging your finger to create a dotted trajectory line to aim and shoot is visually mapped and functional.
*   **Interior layout:** The spatial positioning of the friendly units inside the castle (skull on top, red cyclops middle, green goblin bottom) matches the original framework.

## ❌ Discrepancies
*   **[P0] Characters missing weapons** — In the playable recording (00:07), characters are completely bare-handed and project bullets magically; in the source (clip2_0023.png), characters distinctly hold and fire specific heavy weapons (e.g., green goblin holds a green bazooka).
*   **[P0] Missing damage text and detailed hit feedback** — The playable shows generic gray smoke puffs upon hits (00:04). The source features specific floating damage numbers (e.g., `-140`) and fiery visual effects accompanied by debris (clip2_0158.png).
*   **[P1] UI styling drastically simplified** — The playable features a rudimentary, flat blue/red bar with tiny UI text (00:00). The source uses a large, center-breaking bubbly "VS" graphic, thick black borders, and prominent bubble-font percentages with thick outer outlines (clip2_0001.png).
*   **[P1] Environment art is generic** — The playable background relies on flat vector shapes and solid colors for dirt/trees (00:00). The source utilizes multi-layered, detailed raster parallax backgrounds including clouds, distant mountains, colored terrain, and distinct jungle foliage (clip2_0001.png).
*   **[P1] Castle hull and track graphics lacking detail** — Playable uses a solid orange block and simple black oval caterpillar tracks (00:00). Source features a highly detailed wood/metal textured hull with arched doors and complex, gear-driven track sprite assemblies (clip2_0001.png).
*   **[P1] Inaccurate enemy bomber entity** — The playable uses a modern mechanical airplane sprite (00:01). The source game uses fantasy black bird sprites carrying bombs (clip2_0001.png/clip2_0046.png).
*   **[P2] Interior texturing and platforms** — Playable floors are simple orange dashed lines set against a flat dark brick grid (00:07). Source interior utilizes detailed wooden plank platform assets set against a darker, fully shaded masonry background (clip2_0023.png).
*   **[P2] Aiming line & cursor style** — Playable uses plain, small white dots and a generic flat hand icon (00:07). Source uses larger, shaded orb dots with a high-fidelity 3D-rendered hand cursor and a cross `X` marker at the start point (clip2_0023.png).

## 🔧 Fix list (ordered by impact)
1.  **Add Character Weapons:** Update the character sprites/skeletons in the interior view to include holding their distinct weapons. Ensure the trajectory line and instantiated projectiles originate from the barrel of these weapons, not the center axis of the character body.
2.  **Overhaul HUD Drawing:** In your Canvas UI rendering, replace the standard text. Draw the custom "VS" sprite overlapping the center of the lifebars. Draw health percentages using a custom web font matching the source, applying a heavy dark stroke (approx 4-6px) and white fill, scaling the font size up by at least 2x compared to current.
3.  **Implement Damage Number System:** Create a floating text particle array for the canvas loop. On impact, spawn text (e.g., `-60`) that translates upwards along the Y-axis by ~50px over ~800ms while tweening opacity to 0. Couple this with a fiery explosion particle emitter rather than single gray clouds.
4.  **Replace Environment Assets:** Swap the flat drawing calls for `drawImage()` calls using raster assets for the background. Stack the sky, distant mountains, cloud layer, and foreground tree layers to replicate the source's depth.
5.  **Update Castle Base Sprites:** Remove the flat `#D66A2B` (approx) orange rectangular fill for the base. Implement the detailed wooden hull asset representing the doors, and swap the basic track shapes for the detailed gear-and-tread sprite sheet.
6.  **Replace Jet with Bird:** Swap out the airplane entity asset for the animated flying bird sprite. Keep speed and pathing identical.
7.  **Refine Interior Canvas Textures:** Update the rendering of the internal platforms to stretch/repeat a wood plank image pattern instead of drawing simple colored strokes. Change the flat backdrop pattern to the shaded brick image asset.
8.  **Polish Aiming Trajectory:** Increase the radius of the aiming dots by 2px, apply a slight radial gradient so they appear spherical, and replace the generic HTML/CSS cursor icon with an explicit `drawImage()` call rendering the 3D shiny hand sprite at the user's touch coordinates.

## 🚫 Cannot assess
*   **Audio SFX / Music:** Neither the webm recording nor the static frames provide audio, so sound replication cannot be verified.
*   **Input Sensitivity:** Cannot determine if the drag threshold, weight, or responsiveness feels the same as the native compiled game.
*   **Framerate/Performance:** Real-world performance on target mobile browsers cannot be evaluated from a pre-recorded video.
*   **End-card/Win State:** The playable video fades to white during a transition at the end, hiding any eventual call-to-action or win/loss screen screens.